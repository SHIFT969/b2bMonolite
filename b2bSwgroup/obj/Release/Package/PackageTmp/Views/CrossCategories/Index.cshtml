@model IEnumerable<b2bSwgroup.Models.CrossCategory>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Main.cshtml";
}


<p>
    @Html.ActionLink("Создать новую", "Create")
    @Html.ActionLink("Загрузить списком", "Load")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Category.Name)
        </th>       
        <th>
            @Html.DisplayNameFor(model => model.IdentifyCategory)
        </th>
        <th></th>
    </tr>    
@foreach (var item in Model)
{
    <tr>
        <td>
            @*@Html.Action("EditRow", "CrossCategories", new { item.Id });*@
            @Html.DisplayFor(modelItem => item.Category.Name)
        </td>      
        <td>
            @Html.DisplayFor(modelItem => item.IdentifyCategory)
        </td>
        <td>
            @Html.ActionLink("Редактировать", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Подробнее", "Details", new { id=item.Id }) |
            @Html.ActionLink("Удалить", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
